TODO:
Bug: double clicking on a .pfx results in an empty window being shown

Fix: If there is only one window open and a project is opened, the blank window should be replaced by the opened one.

Fix command-line interface to work properly, especially install paths

Save some of the package info once entered -- Contact name, e-mail, and website.

App Version in PkgInfoWindow should probably default to 0.1 if not already available

Add a bin type for dependencies that will search for a command in one of the standard bin folders

Add an Alert for failed file opens

Implement custom directory handling in the ItemSelected code in PkgWindow
